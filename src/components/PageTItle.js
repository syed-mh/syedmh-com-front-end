const PageTitle = ({ title }) => {
  return (
    <section className='archive-page-opener rounded'>
      <span className='inner'>
        <h1 className='title'>
          {title}
        </h1>
      </span>
    </section>
  )
}

export default PageTitle